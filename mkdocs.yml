site_name: Locator Creator
repo_url: https://github.com/matthewlee-dev/locator-creator
repo_name: matthewlee-dev/locator-creator
theme:
  name: material
  language: en
  logo: resources/images/locator.png
  favicon: resources/images/locator.png
  palette:
    - scheme: slate
      primary: black
      accent: teal

plugins:
- search:
- mkdocstrings:
    default_handler: python
    handlers:
      python:
        # Docs CI syncs only the docs group, so the project isn't installed.
        paths: [src]
        options:
          show_source: true

watch:
  - src

nav:
  - index.md
  - reference.md
